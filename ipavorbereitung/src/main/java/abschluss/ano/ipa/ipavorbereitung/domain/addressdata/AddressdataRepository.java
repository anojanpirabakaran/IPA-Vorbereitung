package abschluss.ano.ipa.ipavorbereitung.domain.addressdata;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AddressdataRepository extends JpaRepository<Addressdata, String> {
}
